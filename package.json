{
  "name": "faisceau",
  "type": "module",
  "version": "0.0.4",
  "description": "Faisceau is a tiny opinionated wrapper around alien-signals that exposes class-based helpers for signals and computed values",
  "author": "lilian1315",
  "license": "MIT",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "LICENSE",
    "README.md",
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "typecheck": "tsc --noEmit",
    "release": "pnpm typecheck && pnpm lint && pnpm build && pnpm bumpp && pnpm publish && pnpm jsr publish"
  },
  "dependencies": {
    "alien-signals": "^3.1.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.2.0",
    "alien-signals": "^3.1.0",
    "bumpp": "^10.3.1",
    "eslint": "^9.39.1",
    "jsr": "^0.13.5",
    "tsdown": "^0.16.6",
    "typescript": "^5.9.3"
  }
}
